# Copy this file to .env and fill in your credentials.
# .env is git-ignored — never commit real keys.

# Exchange API credentials
EXCHANGE_API_KEY=
EXCHANGE_API_SECRET=

# Optional: some exchanges require a passphrase
EXCHANGE_API_PASSPHRASE=
